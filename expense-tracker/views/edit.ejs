<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit expenses page</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css"
    />
  </head>
  <body class="container-fluid">
    <h2>Edit your expense details:</h2>
    <form
      action="/expenses/<%= expenseInfo.id %>?_method=PATCH"
      method="post"
      style="width: 400px; margin-top: 20px"
    >
      <div class="mb-3">
        <label for="amount" class="form-label"><b>Amount</b></label>
        <input
          type="number"
          placeholder="Enter amount..."
          id="amount"
          name="amount"
          class="form-control"
          value="<%= expenseInfo.amount %>"
          required
        />
      </div>
      <div class="mb-3">
        <label for="category" class="form-label"><b>Category</b></label>
        <input
          type="text"
          placeholder="category..."
          id="category"
          name="category"
          class="form-control"
          value="<%= expenseInfo.category %>"
          required
        />
      </div>
      <div class="mb-3">
        <label for="date" class="form-label"><b>Spend Date</b></label>
        <input
          type="date"
          class="form-control"
          name="spend_date"
          id="date"
          value="<%= expenseInfo.spend_date.toISOString().split('T')[0] %>"
          required
        />
      </div>
      <button class="btn btn-success" style="margin: 10px">Continue</button>
      <a href="/expenses" class="btn btn-danger">Cancel</a>
    </form>
  </body>
</html>
